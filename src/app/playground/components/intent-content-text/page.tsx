import PlaygroundIntentContentTextClient from "./PlaygroundIntentContentTextClient";

export default function Page() {
    return (
        <main className="container section pt-10 relative z-10">
            <PlaygroundIntentContentTextClient />
        </main>
    );
}
