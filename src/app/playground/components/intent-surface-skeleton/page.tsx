import PlaygroundIntentSurfaceSkeletonClient from "./PlaygroundIntentSurfaceSkeletonClient";

export default function Page() {
    return (
        <main className="container section pt-10 relative z-10">
            <PlaygroundIntentSurfaceSkeletonClient />
        </main>
    );
}
