import PlaygroundIntentControlButtonGroupClient from "./PlaygroundIntentControlButtonGroupClient";

export default function Page() {
    return (
        <main className="container section pt-10 relative z-10">
            <PlaygroundIntentControlButtonGroupClient />
        </main>
    );
}
